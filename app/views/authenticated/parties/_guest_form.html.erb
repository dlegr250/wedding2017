<hr class="spacer small" />

<%= f.fields_for :guests do |guest_form| %>
  <div class="list-item no-border-bottom">
    <div class="list-item-primary">
      <%= guest_form.text_field :first_name, size: 8 %>
      <%= guest_form.text_field :last_name, size: 12 %>
    </div>
  </div>
  <div class="list-item">
    <div class="list-item-primary">
      <%#= collection_check_boxes(guest, :alcoholic_beverage_ids, AlcoholicBeverage.alphabetical, :id, :name) do |b| %>
        <div class="checkbox">
          <%#= b.check_box %> <%#= b.label %>
        </div>
      <%# end %>
    </div>
  </div>
<% end %>

<hr class="divider small" />
